{% for alert in app.session.flashbag.get('notice') %} 
    {{ include('utils/alert.html.twig', { 'type' : 'info', 'alert': alert }) }} 
{% endfor %}
{% for alert in app.session.flashbag.get('success') %} 
    {{ include('utils/alert.html.twig', { 'type' : 'success', 'alert': alert }) }} 
{% endfor %}
{% for alert in app.session.flashbag.get('error') %} 
    {{ include('utils/alert.html.twig', { 'type' : 'error', 'alert': alert }) }} 
{% endfor %}
{% for alert in app.session.flashbag.get('warning') %} 
    {{ include('utils/alert.html.twig', { 'type' : 'warning', 'alert': alert }) }} 
{% endfor %}
