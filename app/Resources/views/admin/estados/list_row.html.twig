<tr class="gradeX">
    <td>{{ estado.nombre }}</td>
    <td class="center">
        <div class="btn-group">
            <button data-toggle="dropdown" class="btn btn-info dropdown-toggle" type="button">Operaciones <span class="caret"></span></button>
            <ul role="menu" class="dropdown-menu">
                <li><a href="{{ path('ver rubrica', {'id': estado.id}) }}">Ver</a></li>
                <li><a href="{{ path('editar rubrica', {'id': estado.id}) }}">Editar</a></li>
                <li><a href="#estadoModal{{estado.id}}" data-toggle="modal">Eliminar</a></li>
            </ul>
        </div>
    </td>
</tr>
<div aria-hidden="true" aria-labelledby="myModalLabel" role="dialog" tabindex="-1" id="estadoModal{{estado.id}}" class="modal fade">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button aria-hidden="true" data-dismiss="modal" class="close" type="button">×</button>
                <h4 class="modal-title">{{ 'estado.views.delete.title'|trans }}</h4>
            </div>
            <div class="modal-body">

                {{ 'estado.views.delete.message'|trans|nl2br }}

            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">{{ 'estado.views.delete.cancel'|trans }}</button>
                <a href="{{ path('eliminar rubrica', {'id': estado.id}) }}"><button type="button" class="btn btn-danger"> {{ 'estado.views.delete.confirm'|trans }}</button></a>
            </div>
        </div>
    </div>
</div>