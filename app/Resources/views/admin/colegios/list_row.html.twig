<tr class="gradeX">
    <td>{{ colegio.nombre }}</td>
    <td>{{ colegio.tipoColegio.nombre }}</td>
    <td>{{ (colegio.director is null ? ('views.sin_asignar' | trans) : colegio.director.nombre ~' '~colegio.director.apellido) }}</td>
    <td class="center">
        <div class="btn-group">
            <button data-toggle="dropdown" class="btn btn-info dropdown-toggle" type="button">Operaciones <span class="caret"></span></button>
            <ul role="menu" class="dropdown-menu">
                <li><a href="{{ path('ver colegio', {'id': colegio.id}) }}">Ver</a></li>
                <li><a href="{{ path('editar colegio', {'id': colegio.id}) }}">Editar</a></li>
                <li><a href="#">Eliminar</a></li>
            </ul>
        </div>
    </td>
</tr>
